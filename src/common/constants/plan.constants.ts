export enum PlanStatus {
    ACTIVE = 'active',
    INACTIVE = 'inactive',
}

export enum PlanInterval {
    MONTHLY = 'month',
    YEARLY = 'year',
}

export enum PlanDuration {
    MONTHLY = 'monthly',
    YEARLY = 'yearly',
}
